# This workflow is disabled - GymBuddy has been migrated to Heroku
# GitHub Pages deployment is no longer used
# New deployment target: https://gymbuddy-frontend.herokuapp.com

name: GitHub Pages Migration Notice

on:
  workflow_dispatch: # Only run manually for info

jobs:
  migration-info:
    runs-on: ubuntu-latest
    steps:
    - name: Migration Notice
      run: |
        echo "GymBuddy has been migrated from GitHub Pages to Heroku"
        echo "New deployment target: https://gymbuddy-frontend.herokuapp.com"
        echo ""
        echo "Benefits of Heroku deployment:"
        echo "- Better routing support for React Router"
        echo "- Environment variable management"
        echo "- Better debugging and logging"
        echo "- Custom server configuration"
        echo "- No base path issues"
        echo ""
        echo "To deploy updates:"
        echo "1. Commit changes to main branch"
        echo "2. Run: git push heroku main"
        echo "3. Monitor: heroku logs --tail"